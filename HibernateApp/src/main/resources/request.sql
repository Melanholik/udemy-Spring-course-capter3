DROP TABLE IF EXISTS Person;

CREATE TABLE IF NOT EXISTS Person(
    id int PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY ,
    name VARCHAR,
    age INT
);

INSERT INTO Person(name, age) VALUES ('Vitaliy', 35);

SELECT * FROM Person;
